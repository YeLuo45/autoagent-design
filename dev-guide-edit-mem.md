---
title: 编辑内存
slug: /dev-guide-edit-memory
---

# 编辑内存

## 概述

本文档介绍如何在 AutoAgent 项目中编辑和修改内存功能。内存系统是 Agent 架构的核心组件，负责存储和检索对话上下文信息。

## 内存类型

### 短期内存 (Short-term Memory)

短期内存用于存储当前会话的临时信息，包括：

- 当前对话历史
- 最近的工具调用结果
- 临时的中间计算结果

### 长期内存 (Long-term Memory)

长期内存用于持久化存储重要信息：

- 用户偏好设置
- 跨会话的上下文信息
- 已学习的知识库

## 编辑内存

### 基本操作

```python
# 读取内存
memory = agent.memory.read(session_id)

# 更新内存
agent.memory.update(session_id, new_content)

# 删除内存
agent.memory.delete(session_id, key)
```

### 注意事项

1. **数据一致性**: 修改内存时需确保数据一致性
2. **并发控制**: 多线程环境下注意加锁
3. **容量限制**: 注意内存使用的容量限制

## 最佳实践

- 定期清理不需要的短期内存
- 重要数据及时写入长期内存
- 使用合适的数据结构优化检索效率

## 故障排除

常见问题及解决方案：

| 问题 | 解决方案 |
|------|----------|
| 内存溢出 | 清理过期数据，增加容量配置 |
| 数据丢失 | 启用持久化备份 |
| 访问延迟 | 优化索引，使用缓存 |
